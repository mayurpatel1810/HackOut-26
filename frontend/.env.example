# Base URL the browser uses for the Spring Boot gateway.
# In development Vite proxies /api, so this is usually left unset.
VITE_API_BASE=/api
# Where the dev proxy forwards /api to.
VITE_API_TARGET=${http://localhost:8081}
